/**
 * Processes nullable optional field updates for ElectroDB patch operations.
 *
 * @deprecated Import from `@/lib/db/nullable-updates` — kept for existing imports during migration.
 */
export { resolveNullableUpdates } from "@/lib/db/nullable-updates";
